# 📋 Лог №20 — Блок доработок #5

**Дата:** 1 мая 2026

---

## Сделано (6 доработок)

1. **Валидация настроек** — числовые значения проверяются при сохранении в админке
2. **Логирование входов** — каждый вход записывается в admin_log (IP, User-Agent)
3. **Permissions-Policy** — заголовок безопасности в .htaccess (камера, микрофон, геолокация отключены)
4. **Анимация счётчиков** — числа на главной и в кабинете анимируются при появлении
5. **Кнопка «Наверх»** — появляется при скролле вниз, плавный скролл наверх
6. **Уведомление при новом отзыве** — владелец сервера получает уведомление

---

## Затронутые файлы

| Файл | Изменение |
|------|-----------|
| `admin/settings.php` | Валидация числовых настроек |
| `includes/auth.php` | Логирование входов |
| `.htaccess` | Permissions-Policy |
| `assets/js/app.js` | Анимация счётчиков, кнопка наверх |
| `review.php` | Уведомление владельцу |
